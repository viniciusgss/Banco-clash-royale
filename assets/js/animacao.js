document.addEventListener('DOMContentLoaded', () => {
    const text = "Warforged Tavern"; // Texto a ser exibido
    const typingElement = document.getElementById('typingEffect');
    typingElement.textContent = text; // Exibe o texto diretamente
});